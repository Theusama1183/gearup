export interface UserProfileProps {
  user: {
    name: string;
    phone: string;
    address: string;
    city: string;
    state: string;
    country: string;
    // Add other fields as necessary
  };
}
